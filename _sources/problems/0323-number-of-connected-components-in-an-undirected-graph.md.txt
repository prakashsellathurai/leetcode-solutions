# 0323-number-of-connected-components-in-an-undirected-graph


Try it on <a href='https://leetcode.com/problems/0323-number-of-connected-components-in-an-undirected-graph'>leetcode</a>

## Description
<div class="description">
<p>You have a graph of <code>n</code> nodes. You are given an integer <code>n</code> and an array <code>edges</code> where <code>edges[i] = [a<sub>i</sub>, b<sub>i</sub>]</code> indicates that there is an edge between <code>a<sub>i</sub></code> and <code>b<sub>i</sub></code> in the graph.</p>

<p>Return <em>the number of connected components in the graph</em>.</p>

<p>&nbsp;</p>
<p><strong class="example">Example 1:</strong></p>
<img alt="" src="https://assets.leetcode.com/uploads/2021/03/14/conn1-graph.jpg" style="width: 382px; height: 222px;" />
<pre>
<strong>Input:</strong> n = 5, edges = [[0,1],[1,2],[3,4]]
<strong>Output:</strong> 2
</pre>

<p><strong class="example">Example 2:</strong></p>
<img alt="" src="https://assets.leetcode.com/uploads/2021/03/14/conn2-graph.jpg" style="width: 382px; height: 222px;" />
<pre>
<strong>Input:</strong> n = 5, edges = [[0,1],[1,2],[2,3],[3,4]]
<strong>Output:</strong> 1
</pre>

<p>&nbsp;</p>
<p><strong>Constraints:</strong></p>

<ul>
	<li><code>1 &lt;= n &lt;= 2000</code></li>
	<li><code>1 &lt;= edges.length &lt;= 5000</code></li>
	<li><code>edges[i] = [a<sub>i</sub>, b<sub>i</sub>]</code></li>
	<li><code>a<sub>i</sub> != b<sub>i</sub></code></li>
	<li>There are no repeated edges.</li>
</ul>

</div>

## Solution(Python)
```Python
class unionFind:
    def __init__(self,n):  # [[2,3],[1,2],[1,3]]
        self.n = n
        self.parent = [i  for i in range(n)] # [0 1 2 3]
        self.size = [1] * n
    #
    def union(self, a, b): #  1 2
        root_a = self.find(a)  # 1
        root_b = self.find(b) # 2
        
        if root_a == root_b:
            return 0
        elif  self.size [root_a] <  self.size[root_b]:
            self.parent[root_b] = root_a #  [0 1 1 2]
            self.size [root_a] += 1
        else:
            self.parent[root_a] = root_b
            self.size [root_b] += 1
        return 1
    def find(self, a): # 0
        if a == self.parent[a]:
            return a
        return self.find(self.parent[a])
    def num_of_componenets(self):
        return len(set(self.parent))

class Solution:
    def countComponents(self, n: int, edges: List[List[int]]) -> int:
        #  n = 5, edges = [[0,1],[1,2],[3,4]]
        # 0 -> 1 -> 2   
        #   3 -> 4
        #  2
        # 
        # uf ->  n
        #     parent = [n]
        #     
        #     union a ,b  
        #      find root of a and b
        #      set root of a as b
        #     find -> a
        #        iterate until parent = a
        #        a
        #     num_of_componenets -> count number of unique parents
        #
        # edges -> edge uf.union 
        # num_of_componenets return
        #  [[0,1],[1,2],[3,4]]
        #   [0 1 2 3 4]
        #   0,1 -> [0 0 2 3 4]
        #   1,2  -> []
        #
        # edge cASE [[2,3],[1,2],[1,3]]
        # 1->   2 -> 3 -> 1   0
        #              

        uf = unionFind(n)
        cnt = n
        for a,b in edges: # [[0,1],[1,2],[3,4]]
            cnt -= uf.union(a,b)

        return cnt


        
```



<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "QAPage",
  "mainEntity": {
    "@type": "Question",
    "name": "323. Number of Connected Components in an Undirected Graph",
    "text": "You have a graph of n nodes. You are given an integer n and an array edges where edges[i] = [ai, bi] indicates that there is an edge between ai and bi in the graph.\nReturn the number of connected components in the graph.\n\u00a0\nExample 1:\n\n\nInput: n = 5, edges = [[0,1],[1,2],[3,4]]\nOutput: 2\n\nExample 2:\n\n\nInput: n = 5, edges = [[0,1],[1,2],[2,3],[3,4]]\nOutput: 1\n\n\u00a0\nConstraints:\n\n1 <= n <= 2000\n1 <= edges.length <= 5000\nedges[i] = [ai, bi]\nai != bi\nThere are no repeated edges.\n\n",
    "url": "https://leetcode.com/problems/0323-number-of-connected-components-in-an-undirected-graph",
    "answerCount": 1,
    "datePublished": "2026-07-08T16:53:50+05:30",
    "author": {
      "@type": "Organization",
      "name": "LeetCode",
      "url": "https://leetcode.com"
    },
    "acceptedAnswer": {
      "@type": "Answer",
      "text": "class unionFind:\n    def __init__(self,n):  # [[2,3],[1,2],[1,3]]\n        self.n = n\n        self.parent = [i  for i in range(n)] # [0 1 2 3]\n        self.size = [1] * n\n    #\n    def union(self, a, b): #  1 2\n        root_a = self.find(a)  # 1\n        root_b = self.find(b) # 2\n        \n        if root_a == root_b:\n            return 0\n        elif  self.size [root_a] <  self.size[root_b]:\n            self.parent[root_b] = root_a #  [0 1 1 2]\n            self.size [root_a] += 1\n        else:\n            self.parent[root_a] = root_b\n            self.size [root_b] += 1\n        return 1\n    def find(self, a): # 0\n        if a == self.parent[a]:\n            return a\n        return self.find(self.parent[a])\n    def num_of_componenets(self):\n        return len(set(self.parent))\n\nclass Solution:\n    def countComponents(self, n: int, edges: List[List[int]]) -> int:\n        #  n = 5, edges = [[0,1],[1,2],[3,4]]\n        # 0 -> 1 -> 2   \n        #   3 -> 4\n        #  2\n        # \n        # uf ->  n\n        #     parent = [n]\n        #     \n        #     union a ,b  \n        #      find root of a and b\n        #      set root of a as b\n        #     find -> a\n        #        iterate until parent = a\n        #        a\n        #     num_of_componenets -> count number of unique parents\n        #\n        # edges -> edge uf.union \n        # num_of_componenets return\n        #  [[0,1],[1,2],[3,4]]\n        #   [0 1 2 3 4]\n        #   0,1 -> [0 0 2 3 4]\n        #   1,2  -> []\n        #\n        # edge cASE [[2,3],[1,2],[1,3]]\n        # 1->   2 -> 3 -> 1   0\n        #              \n\n        uf = unionFind(n)\n        cnt = n\n        for a,b in edges: # [[0,1],[1,2],[3,4]]\n            cnt -= uf.union(a,b)\n\n        return cnt\n\n\n        ",
      "url": "https://prakashsellathurai.com/leetcode-solutions/problems/0323-number-of-connected-components-in-an-undirected-graph/",
      "datePublished": "2026-07-08T16:53:50+05:30",
      "upvoteCount": 0,
      "author": {
        "@type": "Person",
        "name": "Prakash Sellathurai",
        "url": "https://github.com/prakashsellathurai"
      }
    }
  }
}
</script>